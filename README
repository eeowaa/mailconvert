To install these tools, either:

1. Add the absolute path of ./bin to your $PATH
2. Move the contents of ./bin to an existing location in your $PATH,
   and move the ./libexec directory to the destination's parent

Credit goes to Ricardo García for writing the mailconvert script:

   <https://rg3.name/200711221740.html>
